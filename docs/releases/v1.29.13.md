*"So somehow the AP names/numbers ended up wrong?"* — Dave, on the same SITE1 report.

The Directional Antenna Installation report showed marker "1" on the floor plan overview but "SITE1-B1-01-01-AP04" in row 1 of the table below. The overview was numbering markers by the .esx's insertion order while the table was sorting alphabetically — so the same AP got two different numbers.

Fix: sort the AP list once at the floor-section level and hand the same sorted array to both the overview and the table. Marker "1" now points to whichever AP appears in table row 1.

Suite `v1.29.12 → v1.29.13`, WD Report `v2.13 → v2.14`.

## Files changed

- `web/assets/js/report.js` — `renderAntennaFloorSection` sorts the floor's APs by name and passes the sorted array to both `renderAntennaOverview` (map numbers) and `renderAntennaApTable` (row numbers). Table-side sort removed since input is now pre-sorted.
- `web/report.html`, `web/assets/versions.json` — version bumps.
