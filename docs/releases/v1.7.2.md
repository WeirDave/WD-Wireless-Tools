# WD Report v1.2 — Cover page with insertable logo, tighter print layout

Two things from real use of v1.7.1:

- v1.7.1 had **large whitespace gaps** in the printed PDF — the per-section padding stacked and the `@page` margin was generous.
- No cover page — every report started right on the AP data. You asked for a **cover with an insertable logo**.

Both fixed.

## Cover page

Toggle it in the sidebar under **Cover page → Include cover page** (on by default). When enabled, the report opens with a full-page cover:

- Logo (if uploaded — see below)
- `Report · AP Placement` brand line (teal, small caps)
- Site name as the large title
- Two big stat blocks: total access points and floor plan count
- Generated date

The cover forces a `page-break-after`, so plans start clean on page 2 of the PDF.

## Insertable logo

New sidebar section with an **Upload logo…** button. Pick any PNG/JPG/SVG/WebP/GIF; the logo is read as a data URL and stored in `localStorage` under `wd-report-logo`. It shows in a small sidebar preview immediately and survives reloads — no need to re-pick every time.

Only if the browser refuses to save (private-mode / full storage) do you get a warn toast saying the logo is session-only for this run.

A **Remove** button clears both the preview and the stored key.

## Whitespace cleanup

Print CSS was too generous. v1.7.2:

- `@page` margin: `0.5in` → `0.35in`
- Section padding (`.rep-doc-head`, `.rep-floor-section`, `.rep-legend`, `.rep-doc-foot`): `12px 0` → `4px 0`
- Section bottom margin: `12px` → `6px`
- Floor title bottom margin: `14px` → `8px`
- AP table cell padding: `7px 8px` → `4px 6px`
- The doc-foot's top margin is now `0` so it doesn't orphan onto its own page.
- When a cover is present, the immediately-following floor section no longer force-breaks (the cover already broke). When there's no cover, the inline header + first floor stay together.

Net effect on the sample report: no more empty half-page under the AP table, and the antenna specs legend fits with the table instead of getting pushed to a new page.

## Files changed

- `web/report.html` — new sidebar **Cover page** section (checkbox + upload/remove buttons + preview slot). Page version chip → `v1.2`.
- `web/assets/js/report.js`:
  - `savedLogo` state loaded from `localStorage` on module init.
  - `updateLogoPreview()`, `pickLogo()`, `clearLogo()` exposed on `window`.
  - `renderCover()` + `renderInlineHeader()` — renderer switches between them by the new `opts.cover` flag.
- `web/assets/wd-tools.css` — `.rep-cover*` styles, `.rep-logo-*` sidebar styles, and a new `@media print` block that overrides the v1.7.0 padding/margin defaults with tighter values.
- `web/pages/hosted-index.html` — version chip → `v1.7.2`.
- `README.md` — Report sub-version → `v1.2`.

## Verification

1. Hard-refresh `/report`.
2. Sidebar → **Upload logo…** → pick a PNG. The sidebar preview shows it; the Remove button appears.
3. Cover-page checkbox stays ticked. Drop the sample `.esx`.
4. Report shows cover as page 1: logo (if uploaded), brand line, site title, AP + floor-plan counts, generated date.
5. Ctrl-P → the cover fills page 1, the floor plan + AP table fills page 2 or spans pages 2–3, no huge gaps.
6. Untick the cover checkbox → the compact inline header comes back.
7. Reload the page → logo persists (from localStorage). Remove clears it.

## Version bumps

- Suite: `v1.7.1 → v1.7.2`
- WD Report: `v1.1 → v1.2`
