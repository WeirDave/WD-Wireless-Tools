**Suite-wide cleanup.** A full dead-code and orphaned-file audit across the whole codebase: removed 8 unused JavaScript functions (Cloud Manager, Squirrel, Quick Walls), 4 unused Python methods/functions in the Cloud Manager backend, ~50 unused CSS selectors and custom properties, and 13 superseded branding image files. Also archived 5 one-off maintenance scripts that had already done their (historical, single-use) job, and removed `New-ProjectFolders.ps1`, fully superseded by Squirrel's own Create Project Folder.

**Squirrel.** Fixed a color-cohesion bug: the "→ floorplans" summary chip on the review screen rendered in default gray instead of the WD blue every other destination chip uses — a leftover from a CSS selector (`.chip-plans`) that never got renamed when the JS was updated to build `.chip-floorplans`.

**Report.** Fixed a color bug in the template gallery: **Predictive Design / AP Placement** and **Interference / Rogue Devices** never had their own accent color and silently fell back to the same blue as **Directional Antenna Installation** — all three cards looked identical. Predictive Design now gets teal; Interference gets purple, its original color from before the v2.17 rename from "Hotspot".

Suite `v1.36.0 → v1.37.0` (cloud 4.13.6 → 4.13.7, walls 7.37 → 7.38, report 2.19 → 2.20, squirrel 1.25.0 → 1.25.1).

## Files changed

- `tools/cloud_manager.py` — removed 4 unused functions/methods (`delete_dataset`, `discriminators_conflict`, `pair_conflicts`, `_esx_in_folder`).
- `web/assets/js/cloud.js` — removed 5 unused functions (`renderSourceCard`, `statusBadge`, `renderEsxRow`, `dismissMatchHelpHint`, `_siteOptionsHtml`).
- `web/assets/js/organizer.js` — removed 1 unused function (`subfolderDisplayName`).
- `web/assets/js/walls.js` — removed 2 unused functions (`setDefaultTemplate`, `getLastTemplate`).
- `web/assets/js/wd-shared.js` — removed an unreachable favicon-swap regex branch (no page ever had a 192×192 `<link>` to trigger it).
- `web/assets/wd-tools.css` — removed ~50 dead selectors/custom properties; fixed `.chip-plans` → `.chip-floorplans`; added `.tpl-predictive` and `.tpl-interference` template-card colors; fully removed the dead `.report-toolbar` token (6 sites) left behind when Report's toolbar moved to `.dz-topbar`/`.report-wiz-topbar`.
- `web/assets/*.png`, `.svg` — removed 13 superseded branding image revisions.
- `scripts/` — removed 5 one-off maintenance scripts that already did their (historical) job: `add_favicon_media.py`, `undouble_favicon.py`, `xss_swap.py`, `guide_dark_mode.py`, `alphabetize_nav.py`.
- `New-ProjectFolders.ps1` — removed, superseded by Squirrel's Create Project Folder.
- `web/assets/versions.json` — version bumps (suite, cloud, walls, report, squirrel).

## Verified

- Full existing suite: `python -m pytest tests/ -q` — 52 tests, all passing.
- CSS brace balance and JS syntax checked programmatically after every edit pass.
- Live-verified in browser: Squirrel's `chip-floorplans` fix (isolated sandbox test folder, computed-style check before/after the fix), Report's 8 template-gallery cards now resolve to 8 distinct colors (computed-style check), and Cloud/Walls/Report/Squirrel all load without new console errors.
